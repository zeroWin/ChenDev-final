<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="mobileMedical.namespace.fileTransmission">

    <EditText
        android:id="@+id/ipet"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/FSend"
        android:text="send"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_toRightOf="@+id/ipet" />

    <Button
        android:id="@+id/FReceive"
        android:text="receive"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/FSend"
        android:layout_alignBottom="@+id/FSend"
        android:layout_toRightOf="@+id/FSend" />

    <Button
        android:id="@+id/resuleDisplay4ecg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/ipet"
        android:text="显示心电结果"/>

    <Button
        android:id="@+id/resuleDisplay4bloodPressure"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/ipet"
        android:layout_toRightOf="@+id/resuleDisplay4ecg"
        android:text="显示血压结果"/>

</RelativeLayout>
